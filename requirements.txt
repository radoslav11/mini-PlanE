# Core dependencies
torch>=2.0.0
torch-geometric>=2.3.0
torch-scatter>=2.1.0

numpy>=1.21.0
tqdm>=4.64.0

# SPQR preprocessing requires Sage. Sage is NOT on PyPI; install via conda:
#   conda install -c conda-forge sage=9.6
# (or via mamba)
sage>=9.6

# Optional: only needed for the Polaris experiments (experiments/train_polaris.py).
# Install with `pip install polaris-lib rdkit` if you want to run those.
# polaris-lib
# rdkit
